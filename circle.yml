machine:
  environment: {}
dependencies:
  override:
    - sudo python setup.py install:
        environment: {}
    - cd tests; nosetests:
        environment: {}
  pre:
    - sudo pip2 install nose:
        environment: {}
test:
  override:
    - 'true'
general:
  build_dir: .
